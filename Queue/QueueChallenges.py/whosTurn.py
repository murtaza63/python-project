from Queue import QueryArray
import Queue

class BoardGameManager:
    def nextPlayer(self):
        person = QueryArray 

        if person:
            return person 
        else:
            return None 
        Queue.enqueue(person)
        return person