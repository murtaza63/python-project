#!/usr/bin/env python

# Copyright (c) 2022 Razeware LLC
# For full license & permission details, see LICENSE.markdown.

"""
 ## 3. Partitioning with Swift Standard Library

 Implement Quicksort using the `partition(by:)` function that is part of the Swift Standard Library.

 > For more information refer to Apple's documentation here: https:#developer.apple.com//documentation//swift//array//3017524-partition

"""

# Does not apply in Python
# def quicksort(array: list[int]) -> None:
#     self.quicksort_lumuto(array, 0, len(array) - 1)

# def quicksort_lomuto(arary, low: int, high: int) -> None:
#   if low <= high:
#     pivot_value = self[high]

#     p = self.partition { $0 > pivot_value }

#     if p == end_index:
#       p = index(before: p)
#     self[:p].quicksort_lumuto(low: low, high: index(before: p))
#     self[p:].quicksort_lumuto(index_after_i(p), high)

# numbers = [12, 0, 3, 9, 2, 21, 18, 27, 1, 5, 8, -1, 8]
# print(numbers)
# quicksort(numbers)
# print(numbers)
